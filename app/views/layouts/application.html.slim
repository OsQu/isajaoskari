doctype html

html
  head
    title =t "title"
    = stylesheet_link_tag 'application', media: 'all'
    = csrf_meta_tags

    meta charset="utf-8"
    meta charset="utf-8"
    meta http-equiv="x-ua-compatible" content="ie=edge"
    meta name="viewport" content="width=device-width, initial-scale=1.0"

body
  div#container
    div.row
      div.columns.medium-10.medium-centered
        ul#navigation.menu
          li class=('active' if active? :wedding)
            a href=weddings_path =t "wedding"
          li class=('active' if active? :kids)
            a href=kids_path =t "kids"
          li class=('active' if active? :wedding_day)
            a href=wedding_day_path =t "wedding_day"
          li class=('active' if active? :accommodation)
            a href=accommodation_path =t "accommodation"
          li class=('active' if active? :contact)
            a href=contact_path =t "contact"

    div.row
      div.columns.medium-8.medium-centered
        div#content
          == yield
